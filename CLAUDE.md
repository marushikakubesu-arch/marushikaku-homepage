# PROJECT RULES

## 基本ルール
- 日本語で出力
- 作業前に必ずCLAUDE.mdを確認する
- 新規ファイル作成前に一覧を提示してユーザーの承認を得る（詳細フローは下記「新規ファイル作成フロー」参照）
- ファイルを勝手に増やさない
- 既存ファイルを優先確認してから作成
- 不要ファイルはarchiveへ退避
- フォルダ構造を変更しない
- ファイル名は英数字とアンダーバーのみ

## セキュリティルール
- APIキーは必ず.envファイルに書く（コードに直書き禁止）
- 顧客情報（氏名・電話番号・住所）をプロンプトに貼らない
- 本番反映前に必ず動作確認
- gitリポジトリを作る場合は必ず.gitignoreを作成する（.env・セッションファイル・ログを除外）
- 外部公開するAPIエンドポイントには必ずトークン認証を実装する
- トークン・シークレットはランダム文字列を生成して使う（推測可能な値は禁止）
- セッションファイル（クッキー等）はgitignoreで除外し、漏洩しないよう管理する
- 本番環境（環境変数・URL等で判別できる本番系）への直接操作・直接テストは行わない

## 新規プロジェクトルール
- ここでいう「新規プロジェクト」は、独立したツール・アプリ単位を指す。既存プロジェクト内の軽微な修正・ファイル追加では新規CLAUDE.mdは作らない
- 新規プロジェクトフォルダを作成したら、必ずそのフォルダ内にCLAUDE.mdを作成すること
- 編集完了後は必ずそのプロジェクトのCLAUDE.mdを最新状態に更新すること
- CLAUDE.mdのフォーマット：概要・技術スタック・重要なID設定値・ファイル構成・設計の意図・TODO・最終更新日の7項目
- 新規プロジェクトフォルダを作成した場合は、本ファイル下部の「プロジェクト一覧」にも1行追記すること

## 新規ファイル作成フロー
1. 既存ファイルを確認
2. 作成予定ファイルの一覧をユーザーに提示
3. 承認を得てから作成


##ファイルの変更が一区切りついたら、コミットメッセージを提案し、
承認を得てからGitHubにpushする

---

# 事業情報（概要）
<!-- 自分の会社・事業の概要を1〜2行で書いてください。複数事業がある場合は事業ごとに分けてOKです -->
<!-- 例：○○業。対象エリア／取扱商品・サービス／収益モデルなど、Claude Codeに知っておいてほしい前提を書く -->

## （就労継続支援B型事業所　まるしかくベース）
- （三重県鈴鹿市で運営する就労継続支援B型事業所。利用者に就労機会や生産活動の場を提供し、作業を通じた支援を行う。）
- （障害福祉サービスの報酬を主な収益とし、生産活動として観葉植物の育成・販売なども行う。）

---

# プロジェクト一覧
<!-- 新規プロジェクトフォルダを作成したら、ここに1行追記すること -->
<!-- 「フォルダ」列には実際のフォルダ名、「内容」列にはそこで何をしているか一言を書く -->

| フォルダ | 内容 |
|---------|------|
| homepage | まるしかくベースの紹介ホームページ（HTML/CSS/JS） |
| （フォルダ名） | （内容を記入） |

<!--
【使い方メモ（このコメントは消してOK）】
・このファイルは「CLAUDE.md」という名前で、普段作業する一番大元のフォルダ直下に保存してください
・Claude Codeは、開いているフォルダだけでなく、その親フォルダのCLAUDE.mdも参照します
・そのため、この大元フォルダの下にプロジェクトごとのサブフォルダを作って作業すれば、
　このルールが自動的に全プロジェクトに適用されます
・「事業情報」「プロジェクト一覧」は空欄のまま使い始めてOKです。プロジェクトが増えるたびに追記していってください
・プロジェクトごとの詳しいルール（使用言語、ファイル構成など）は、
　各プロジェクトのフォルダに別のCLAUDE.mdを置いて管理してください（新規プロジェクトルール参照）
-->